# Dockerfile for Todo App Backend
FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY requirements.txt ./

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "src.api.todo_api:app", "--host", "0.0.0.0", "--port", "8000"]